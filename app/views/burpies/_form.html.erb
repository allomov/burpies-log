<%= form_with(model: burpy, local: true) do |form| %>
  <% if burpy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(burpy.errors.count, "error") %> prohibited this burpy from being saved:</h2>

      <ul>
      <% burpy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :reason %>
    <%= form.text_field :reason %>
  </div>

  <div class="field">
    <%= form.label :person %>
    <%= form.text_field :person %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
